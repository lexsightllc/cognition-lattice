#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"

rm -rf .pytest_cache .mypy_cache ruff_cache dist build .build htmlcov
find src tests -type d -name "__pycache__" -prune -exec rm -rf {} +
