#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"

PYTHONPATH="${ROOT_DIR}/src:${PYTHONPATH:-}" \
  pytest --cov=cognition_lattice --cov-report=term-missing --cov-report=xml "$@"
