#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"

OUTPUT_DIR="${ROOT_DIR}/docs/api"
mkdir -p "${OUTPUT_DIR}"

PYTHONPATH="${ROOT_DIR}/src:${PYTHONPATH:-}" \
  python -m pdoc --html --output-dir "${OUTPUT_DIR}" cognition_lattice
